import React from 'react'

export default function ResponseList(props) {

// pass in mainResponseList, SurveyId then we can just filter it and display the filtered list of responses
// then import this component to SurveyDetails?

  return (
    <div>
      
      <p>this is where the responses will go</p>
    </div>
  )
}
